name: kripto_alarm
description: "Kripto Alarm Merkezi - Piyasayı anlık takip et, fırsatları kaçırma"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  shared_preferences: ^2.2.3
  workmanager: ^0.5.2
  flutter_local_notifications: ^17.1.2
  fl_chart: ^0.68.0
  provider: ^6.1.2
  intl: ^0.19.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
